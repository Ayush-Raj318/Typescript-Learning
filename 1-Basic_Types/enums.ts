
// Enumerations or Enums

enum UserRoles{
    ADMIN = "admin",
    GUEST = "guest",
    SUPER_ADMIN = "super_admin"
}

enum StatusCodes{
    ABANDONED = "abondoned status code is 500",
    NOT_FOUND = "NOT FOUNT IS 400"
}
